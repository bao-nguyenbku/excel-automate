/** Max tab clicks while waiting for the Intended practices section heading (styled MUI h2). */
export const INTENDED_PRACTICES_NAV_ATTEMPTS = 5;

export const INTENDED_PRACTICES_HEADING_SEL = '.sc-eldPxv.cKoRHP.MuiTypography-root.MuiTypography-h2';

/** Intended practices stage label in data-testid / aria-label — only one locale is in the DOM at a time. */
export const INTENDED_PRACTICES_STAGE_LABELS = ['Intended practices', 'Phương thức can thiệp canh tác dự kiến'];

export const INTENDED_PRACTICES_LOCKED_ALERT_PATTERN =
  /This section is locked|already enrolled in Vietnam TRVC Rice|Phần này bị khóa vì bạn đã đăng ký vào Vietnam TRVC Rice Season 5\./i;
